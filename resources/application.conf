ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ se.kth.somabits.ApplicationKt.module ]
        defaultDiscoveryServices = [ "_osc._udp.local." ]
    }
}
